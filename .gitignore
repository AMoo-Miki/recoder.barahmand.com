/*.cmd
/node_modules
.playwright-mcp/
test-results.json

# Playwright Test artifacts (HTML report, traces, downloaded files)
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Debugging/screenshot artifacts at the repo root.
# (img/*.png and other tracked assets are unaffected.)
/*.png
/*.jpg
/*.jpeg

# Vitest coverage output
/coverage

# Build output (rebuilt by CI on every push)
/dist
